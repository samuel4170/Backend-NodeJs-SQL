import { Express } from "express";
import cursosrouter from ("../routest")

require("../router/get");


app.get
const app = Express();

//listar rutas
app.cursosrouter(liten.application)

app.listen(3000, () => {
  console.log("puero 300 abierto!");
});